function add(a, b) {
    const sum = a + b;
    return sum;
}
function subtract(a, b) {
    const subtrahend = a - b;
    return subtrahend;
}
function multiply(a, b) {
    const product = a * b;
    return product;
}
function divide(a, b) {
    const remainder = a / b;
    return remainder;
}